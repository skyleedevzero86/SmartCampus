<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sleekydz86.server.market.product.infrastructure.mapper.ProductLikeMapper">

    <resultMap id="ProductLikeResultMap" type="com.sleekydz86.server.market.product.domain.ProductLike">
        <id property="id" column="id"/>
        <result property="memberId" column="member_id"/>
        <result property="productId" column="product_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="existsByProductIdAndMemberId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM product_like
        WHERE product_id = #{productId} AND member_id = #{memberId}
    </select>

    <select id="executeProductLikeCUD" statementType="CALLABLE" parameterType="map">
        {CALL sp_product_like_cud(
            #{operation, mode=IN, jdbcType=CHAR},
            #{id, mode=IN, jdbcType=BIGINT},
            #{memberId, mode=IN, jdbcType=BIGINT},
            #{productId, mode=IN, jdbcType=BIGINT},
            #{resultMessage, mode=OUT, jdbcType=VARCHAR},
            #{affectedRows, mode=OUT, jdbcType=INTEGER}
        )}
    </select>

</mapper>
