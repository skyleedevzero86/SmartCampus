<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sleekydz86.server.market.member.infrastructure.mapper.TradeHistoryMapper">

    <resultMap id="TradeHistoryResultMap" type="com.sleekydz86.server.market.member.domain.member.TradeHistory">
        <id property="id" column="id"/>
        <result property="buyerId" column="buyer_id"/>
        <result property="sellerId" column="seller_id"/>
        <result property="productId" column="product_id"/>
        <result property="productOriginPrice" column="product_origin_price"/>
        <result property="productDiscountPrice" column="product_discount_price"/>
        <result property="usingCouponIds" column="using_coupon_ids"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAllByBuyerId" resultMap="TradeHistoryResultMap">
        SELECT id, buyer_id, seller_id, product_id, product_origin_price, product_discount_price, using_coupon_ids, created_at, updated_at
        FROM trade_history
        WHERE buyer_id = #{buyerId}
    </select>

    <select id="findAllBySellerId" resultMap="TradeHistoryResultMap">
        SELECT id, buyer_id, seller_id, product_id, product_origin_price, product_discount_price, using_coupon_ids, created_at, updated_at
        FROM trade_history
        WHERE seller_id = #{sellerId}
    </select>

    <select id="executeTradeHistoryCUD" statementType="CALLABLE" parameterType="map">
        {CALL sp_trade_history_cud(
            #{operation, mode=IN, jdbcType=CHAR},
            #{id, mode=IN, jdbcType=BIGINT},
            #{buyerId, mode=IN, jdbcType=BIGINT},
            #{sellerId, mode=IN, jdbcType=BIGINT},
            #{productId, mode=IN, jdbcType=BIGINT},
            #{productOriginPrice, mode=IN, jdbcType=INTEGER},
            #{productDiscountPrice, mode=IN, jdbcType=INTEGER},
            #{usingCouponIds, mode=IN, jdbcType=VARCHAR},
            #{resultMessage, mode=OUT, jdbcType=VARCHAR},
            #{affectedRows, mode=OUT, jdbcType=INTEGER}
        )}
    </select>

</mapper>
